# @shieldscan/web-scanner

免費檢測網站（Next.js 15 + React 19）。

- `/` — 一鍵掃描首頁（MVP 雛形，已串接 browser-sdk 採集模組）
- 規劃：`/scan` 檢測頁、`/scan/{id}` 結果詳情、`/compare` 多指紋對比、`/docs` 開發者文件

```bash
pnpm --filter @shieldscan/web-scanner dev
```
