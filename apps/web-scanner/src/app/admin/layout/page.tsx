import BlockLayoutEditor from '../../../components/admin/block-layout-editor';

export const metadata = { title: '版面設定（Phase A 試用）' };

export default function AdminLayoutPage() {
  return <BlockLayoutEditor />;
}
