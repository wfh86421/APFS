# 瀏覽器指紋檢測平台：0-1 插件化核心平台架構規劃

## 核心理念

從 0-1 開始就不要把它設計成單一網站，而要設計成「環境可信度 Harness」。

產品形態是：

```text
核心平台 + 可插拔檢測模組 + 可插拔評分規則 + 可插拔輸出渠道 + 多平台協同 SDK
```

也就是「一切皆插件」：

- Web 指紋檢測是插件。
- Android Root/雙開偵測是插件。
- iOS Jailbreak/App Attest 是插件。
- DNS Leak 是插件。
- Port Scan 是插件。
- 看劇平台帳號共享風險是插件。
- 金融登入風控規則是插件。
- Dashboard、Webhook、PDF、JSON、API response 也是輸出插件。

平台核心只負責標準化、調度、驗證、儲存、評分、審計、權限與商業化。

## 0-1 最高原則

1. 資料契約先於功能。
2. 插件協議先於具體插件。
3. 核心平台不依賴任何單一檢測技術。
4. 每個檢測能力都必須可版本化、可啟用、可停用、可替換。
5. 每個評分規則都必須可配置、可解釋、可回溯。
6. 每個輸出渠道都必須可掛載、可限權、可審計。
7. Web、Android、iOS、WebView、後端 API 都回到同一份 `EnvironmentReport`。

## 目標架構

```text
Client / Runtime Layer
  ├─ Browser SDK
  ├─ Android SDK
  ├─ iOS SDK
  ├─ WebView / Hybrid SDK
  ├─ Node / Server SDK
  └─ REST API Direct Ingestion

Plugin Layer
  ├─ Detection Plugins
  ├─ Analysis Plugins
  ├─ Scoring Plugins
  ├─ Policy Plugins
  └─ Output Plugins

Core Harness
  ├─ Plugin Registry
  ├─ Execution Orchestrator
  ├─ Report Normalizer
  ├─ Trust & Signature Verifier
  ├─ Scoring Engine
  ├─ Policy Engine
  ├─ Tenant / API Key / Billing
  ├─ Audit Log
  └─ Data Governance

Data Platform
  ├─ PostgreSQL
  ├─ Redis
  ├─ ClickHouse / TimescaleDB
  ├─ Object Storage
  └─ Feature Store

Commercial Surface
  ├─ Free Scanner Website
  ├─ Developer Portal
  ├─ SaaS Dashboard
  ├─ Risk API
  ├─ Webhook
  ├─ Reports
  └─ Enterprise Private Deployment
```

## 核心平台職責

核心平台不直接知道「Canvas 怎麼檢測」或「Android 雙開怎麼判斷」。核心只知道：

- 有哪些插件。
- 插件在哪些平台可用。
- 插件需要什麼權限。
- 插件輸入輸出格式。
- 插件版本。
- 插件是否可信。
- 插件結果如何合併到報告。
- 哪些租戶可使用哪些插件。
- 哪些規則要套用到哪些場景。
- 結果要輸出到哪裡。

### Core Harness 模組

| 模組 | 職責 |
|---|---|
| Plugin Registry | 管理插件清單、版本、平台、權限、狀態 |
| Execution Orchestrator | 根據平台、租戶、策略選擇要執行的插件 |
| Report Normalizer | 將不同端回傳的訊號轉成標準報告格式 |
| Trust Verifier | 驗證 SDK 簽名、時間戳、nonce、上報完整性 |
| Scoring Engine | 套用評分插件與權重 |
| Policy Engine | 依客戶場景做 allow / review / block / challenge |
| Output Router | 將結果輸出到 API、Webhook、Dashboard、PDF、JSON |
| Data Governance | 管理匿名化、保留期限、刪除請求、審計 |
| Tenant System | 管理客戶、API key、配額、計費、權限 |

## 插件分類

### 1. Detection Plugin

負責採集原始訊號。

例子：

- `browser.canvas`
- `browser.webgl`
- `browser.audio`
- `browser.webrtc`
- `network.dnsLeak`
- `network.ipReputation`
- `network.portProbe`
- `mobile.android.root`
- `mobile.android.cloneSpace`
- `mobile.android.emulator`
- `mobile.ios.jailbreak`
- `mobile.ios.appAttest`
- `app.webview.consistency`
- `content.streaming.accountSharing`

### 2. Analysis Plugin

負責把多個原始訊號組合成判斷。

例子：

- `analysis.osMismatch`
- `analysis.timezoneMismatch`
- `analysis.languageMismatch`
- `analysis.gpuDeviceMismatch`
- `analysis.webNativeMismatch`
- `analysis.proxyVpnTor`
- `analysis.automationRuntime`

### 3. Scoring Plugin

負責產生分數。

例子：

- `score.privacyExposure`
- `score.authenticity`
- `score.automationRisk`
- `score.networkTrust`
- `score.mobileIntegrity`
- `score.streamingAbuse`
- `score.financialFraud`

### 4. Policy Plugin

負責把分數轉成商業決策。

例子：

- `policy.loginRisk`
- `policy.registrationRisk`
- `policy.paymentRisk`
- `policy.streamingPlaybackRisk`
- `policy.gameAntiCheat`
- `policy.couponAbuse`

輸出可為：

- `allow`
- `review`
- `challenge`
- `limit`
- `block`
- `log_only`

### 5. Output Plugin

負責結果輸出。

例子：

- `output.apiJson`
- `output.webhook`
- `output.dashboard`
- `output.pdf`
- `output.jsonExport`
- `output.auditLog`
- `output.siem`
- `output.slack`

## 插件標準介面

### 插件 Manifest

每個插件都必須有 manifest。

```ts
export interface PluginManifest {
  id: string;
  name: string;
  version: string;
  type: 'detection' | 'analysis' | 'scoring' | 'policy' | 'output';
  platforms: Platform[];
  capabilities: string[];
  requiredPermissions: string[];
  inputSchema: string;
  outputSchema: string;
  riskLevel: 'low' | 'medium' | 'high';
  defaultEnabled: boolean;
}

export type Platform =
  | 'browser'
  | 'android'
  | 'ios'
  | 'webview'
  | 'node'
  | 'edge'
  | 'server';
```

### Detection Plugin 介面

```ts
export interface DetectionPlugin<TContext, TResult> {
  manifest: PluginManifest;
  isSupported(context: TContext): Promise<boolean> | boolean;
  collect(context: TContext): Promise<TResult>;
  normalize(result: TResult): Promise<NormalizedSignal>;
}
```

### Analysis Plugin 介面

```ts
export interface AnalysisPlugin {
  manifest: PluginManifest;
  analyze(report: EnvironmentReport): Promise<AnalysisIssue[]>;
}
```

### Scoring Plugin 介面

```ts
export interface ScoringPlugin {
  manifest: PluginManifest;
  score(report: EnvironmentReport, issues: AnalysisIssue[]): Promise<ScoreResult>;
}
```

### Policy Plugin 介面

```ts
export interface PolicyPlugin {
  manifest: PluginManifest;
  decide(input: PolicyInput): Promise<PolicyDecision>;
}
```

### Output Plugin 介面

```ts
export interface OutputPlugin {
  manifest: PluginManifest;
  deliver(input: OutputInput): Promise<OutputResult>;
}
```

## 統一資料契約

所有平台都必須回到同一份 `EnvironmentReport`。

```ts
export interface EnvironmentReport {
  reportId: string;
  tenantId?: string;
  sessionId: string;
  subjectId?: string;
  source: ReportSource;
  createdAt: string;
  consent: ConsentState;
  sdk: SdkInfo;
  signals: NormalizedSignal[];
  issues: AnalysisIssue[];
  scores: ScoreBundle;
  policy?: PolicyDecision;
  outputs?: OutputResult[];
  integrity: ReportIntegrity;
}
```

### NormalizedSignal

```ts
export interface NormalizedSignal {
  id: string;
  pluginId: string;
  pluginVersion: string;
  platform: Platform;
  category:
    | 'browser'
    | 'hardware'
    | 'network'
    | 'security'
    | 'mobile'
    | 'app'
    | 'content';
  key: string;
  value: unknown;
  hash?: string;
  confidence: number;
  collectedAt: string;
}
```

### ScoreBundle

```ts
export interface ScoreBundle {
  privacyExposure: number;
  authenticity: number;
  automationRisk: number;
  networkTrust: number;
  mobileIntegrity?: number;
  contentAbuseRisk?: number;
  custom?: Record<string, number>;
}
```

## 多平台協同設計

### Browser

可採集：

- Canvas、WebGL、WebGPU、Audio、Fonts。
- UA、Client Hints、Timezone、Language。
- Screen、Touch、Memory、CPU。
- WebRTC/STUN。
- Cookie、DNT、Permissions、Media Devices。

限制：

- 不能可靠做端口掃描。
- 不能取得真正硬體序號。
- 容易被隱私瀏覽器或指紋瀏覽器干擾。

### Android

可採集：

- Build props、ABI、CPU、Screen、Sensor。
- Root、Magisk、Xposed、Frida。
- Emulator、Clone Space、Virtual App、Work Profile。
- VPN/Proxy、DNS、WebView 版本。
- App signature、installer source、debuggable flag。

重點：

- Android 是偵測雙開、模擬器、Hook、重打包的主戰場。
- 必須設計 Native SDK + server challenge，避免只靠本地判斷。

### iOS

可採集：

- DeviceCheck。
- App Attest。
- Jailbreak indicators。
- VPN/Proxy。
- WebView/Safari 差異。
- 系統版本、語言、地區、時區。

重點：

- iOS 權限較封閉，不能追求 Android 式深層訊號。
- 可信度重點應放在 Apple 官方 attestation、越獄跡象與行為一致性。

### WebView / Hybrid App

可採集：

- WebView UA。
- Native SDK signals。
- JS SDK signals。
- 後端 HTTP headers。
- App session / device session 對應。

重點：

- WebView 是很多看劇、電商、遊戲、任務平台的核心入口。
- 最有價值的是「Native、WebView、Server 三方訊號是否一致」。

### Server / Node

可採集：

- HTTP headers。
- TLS fingerprint。
- IP/ASN/GeoIP。
- Request timing。
- API key / tenant / session context。
- Backend risk decision。

重點：

- 後端 SDK 用來驗證前端 SDK 上報是否可信。
- 高價值客戶會把它放在登入、註冊、支付、播放、下單流程。

## 插件執行流程

```text
1. Client 初始化 SDK
2. SDK 向核心平台取得 plugin profile
3. Orchestrator 根據平台/租戶/場景回傳插件清單
4. Client 執行 Detection Plugins
5. Client 對結果簽名並上報
6. API Gateway 驗證 API key、nonce、timestamp、signature
7. Report Normalizer 產生 EnvironmentReport
8. Analysis Plugins 產生 Issues
9. Scoring Plugins 產生 ScoreBundle
10. Policy Plugins 產生決策
11. Output Plugins 輸出 API/Webhook/Dashboard/PDF/Audit
12. Data Platform 儲存匿名化與可審計資料
```

## 插件熱插拔機制

### 啟用與停用

插件不應寫死在程式碼流程中，而是由設定控制。

```ts
export interface PluginProfile {
  profileId: string;
  tenantId: string;
  scenario: 'scanner' | 'login' | 'payment' | 'streaming' | 'game' | 'custom';
  enabledPlugins: string[];
  disabledPlugins: string[];
  scoringProfile: string;
  outputProfile: string;
}
```

### 灰度發布

每個插件要支援：

- 全量啟用。
- 指定租戶啟用。
- 指定百分比流量啟用。
- 指定平台啟用。
- 指定版本鎖定。
- 快速 rollback。

### 插件版本策略

版本格式：

```text
plugin-id@major.minor.patch
```

例如：

```text
browser.canvas@1.0.0
mobile.android.cloneSpace@0.3.0
score.streamingAbuse@1.2.0
```

資料入庫必須記錄插件版本，否則未來無法回溯「某個分數是由哪版規則算出來的」。

## 可插拔評分規則

不同產業不要共用同一套權重。

### 通用隱私檢測

重視：

- Privacy Exposure。
- DNS Leak。
- Canvas Tampering。
- Timezone/Language mismatch。

### 金融/支付

重視：

- Authenticity。
- Automation Risk。
- Proxy/VPN/Tor。
- Emulator/Root/Jailbreak。
- 指紋短期大量變化。

### 看劇平台/內容平台

重視：

- Account Sharing Risk。
- 地區限制繞過。
- VPN/Proxy。
- WebView/播放器異常。
- 同帳號多設備/多地區/多 ASN 切換。
- 自動化抓取與批量播放。

### 遊戲/任務平台

重視：

- Emulator。
- Multi-instance。
- Hook/Frida/Xposed。
- Automation Runtime。
- 多帳號批量行為。

### 評分設定範例

```json
{
  "profile": "streaming-default",
  "weights": {
    "network.proxyVpn": 20,
    "content.accountSharing": 25,
    "app.webviewMismatch": 15,
    "automation.runtime": 15,
    "geo.regionMismatch": 10,
    "device.multiAccountCluster": 15
  },
  "thresholds": {
    "allow": 30,
    "review": 60,
    "challenge": 75,
    "block": 90
  }
}
```

## 可插拔輸出渠道

同一份報告可輸出成不同產品形態。

### 即時 API

用於登入、支付、播放前檢查。

### Webhook

用於風險事件通知。

### Dashboard

用於企業查詢、審計、趨勢分析。

### PDF / JSON

用於一般使用者報告匯出。

### SIEM / Data Warehouse

用於大型企業資安與資料團隊。

## 0-1 開發順序

### Step 1：定義資料契約

先完成：

- `EnvironmentReport`
- `NormalizedSignal`
- `AnalysisIssue`
- `ScoreBundle`
- `PolicyDecision`
- `PluginManifest`

理由：

- 這是所有 SDK、API、資料庫、Dashboard 的共同語言。

### Step 2：建立 Plugin Registry

先支援本地靜態 registry，後續再升級成資料庫/後台管理。

理由：

- 0-1 不需要一開始做完整插件市場，但要先讓架構能熱插拔。

### Step 3：建立 Browser SDK MVP

內建插件：

- `browser.ua`
- `browser.canvas`
- `browser.webgl`
- `browser.audio`
- `browser.screen`
- `browser.locale`
- `browser.timezone`
- `browser.webrtc`

理由：

- Web 端最容易快速驗證。

### Step 4：建立 Core API

API：

```text
GET  /v1/plugin-profile
POST /v1/reports
POST /v1/analyze
GET  /v1/reports/{id}
```

理由：

- 先打通 SDK → API → Report → Score 的閉環。

### Step 5：建立免費檢測網站

功能：

- 一鍵掃描。
- 報告分區展示。
- 風險解釋。
- JSON 匯出。
- 開發者文件入口。

理由：

- 它是資料入口、展示入口、SEO 入口、商業入口。

### Step 6：建立 Scoring Profile

先做三套：

- `privacy-default`
- `authenticity-default`
- `streaming-default`

理由：

- 早期就能證明「同一份資料，不同場景有不同價值」。

### Step 7：建立 Android SDK Alpha

先做：

- emulator detection。
- root indicators。
- clone space indicators。
- proxy/vpn indicators。
- app signature。
- WebView bridge。

理由：

- Android 是雙開空間、模擬器、Hook 的核心戰場。

### Step 8：建立看劇平台 Demo

Demo 流程：

```text
使用者登入 → SDK 採集 → API 分析 → 播放前決策 → allow/challenge/block
```

理由：

- 看劇平台是很適合展示商業價值的場景：帳號共享、地區限制、VPN、播放器異常、批量播放都能串起來。

## 建議 Monorepo 結構

```text
environment-trust-platform/
  apps/
    web-scanner/
    developer-portal/
    dashboard/
    streaming-demo/
    api/
  packages/
    core-schema/
    plugin-runtime/
    scoring-engine/
    policy-engine/
    browser-sdk/
    react-sdk/
    node-sdk/
  mobile/
    android-sdk/
    ios-sdk/
    demo-android/
    demo-ios/
  plugins/
    detection/
      browser/
      network/
      android/
      ios/
      webview/
    analysis/
    scoring/
    policy/
    output/
  services/
    ingestion-api/
    scanner-service/
    dns-stun-service/
    reputation-service/
  infra/
    docker/
    terraform/
  docs/
    architecture/
    api/
    sdk/
    compliance/
```

## MVP 插件清單排序

### 第一批：Web 快速閉環

1. `browser.ua`
2. `browser.clientHints`
3. `browser.canvas`
4. `browser.webgl`
5. `browser.audio`
6. `browser.screen`
7. `browser.locale`
8. `browser.timezone`
9. `browser.webrtc`

理由：

- 最容易做出可展示結果。
- 與現有 browserscan.net 報告最接近。
- 可快速累積第一批真實資料。

### 第二批：Network 商業價值

1. `network.ipGeo`
2. `network.asnIsp`
3. `network.proxyVpn`
4. `network.dnsLeak`
5. `network.ipReputation`
6. `network.portProbe`

理由：

- 直接提升企業風控價值。
- 可支援看劇、金融、電商、防爬蟲。

### 第三批：Mobile 護城河

1. `mobile.android.emulator`
2. `mobile.android.root`
3. `mobile.android.cloneSpace`
4. `mobile.android.hook`
5. `mobile.android.appSignature`
6. `mobile.ios.jailbreak`
7. `mobile.ios.appAttest`
8. `app.webview.consistency`

理由：

- 單純 Web 平台容易被複製。
- Mobile + WebView + Server 三方一致性會大幅提高替代門檻。

### 第四批：場景化插件

1. `content.streaming.accountSharing`
2. `content.streaming.geoBypass`
3. `content.streaming.playerIntegrity`
4. `policy.streamingPlaybackRisk`
5. `policy.paymentRisk`
6. `policy.gameAntiCheat`

理由：

- 場景化插件直接對應客戶痛點，也最容易定價。

## 風險與邊界

### 法務與合規

- 必須明確告知資料用途。
- 預設匿名化。
- 支援資料刪除請求。
- 端口檢測必須 opt-in，且只檢測使用者自身來源 IP。
- 行動端深層檢測要避免越權與暗中收集。

### 技術風險

- 瀏覽器 API 會變動。
- iOS 可採集訊號有限。
- 指紋瀏覽器會持續對抗。
- SDK 被逆向是必然，要以 server challenge、簽名、行為一致性降低風險。

### 商業風險

- 初期資料量不足，不能過早宣稱精準風險資料庫。
- 企業客戶需要可解釋性，不能只給黑盒分數。
- 不同產業權重不同，必須靠 scoring profile 解決。

## 最終結論

0-1 最正確的做法是：

```text
先做插件協議，再做第一批插件。
先做統一資料契約，再做網站畫面。
先做 SDK/API 閉環，再做企業 Dashboard。
先讓 Web 跑起來，再把 Android/iOS/WebView 接進來。
```

這樣平台會自然長成一個「多平台協同、一切皆插件」的環境可信度 Harness。短期它是一個瀏覽器指紋檢測網站；中期它是 SDK/API 風控平台；長期它是跨端真實環境資料與風險判斷的商業基礎設施。
