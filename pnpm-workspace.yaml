packages:
  - apps/*
  - packages/*
  - examples/*

# pnpm 11 注意：
# - strictDepBuilds 預設 true：未允許的依賴 postinstall 會讓安裝「直接失敗」。
# - Docker 建置只需要 tsc / next 編譯，不需要 embedded-postgres /
#   redis-memory-server 下載原生二進位，因此關閉嚴格模式並僅允許 esbuild。
strictDepBuilds: false

allowBuilds:
  esbuild: true
